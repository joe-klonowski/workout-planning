// Legacy .eslintrc (kept for compatibility) — ESLint v9+ uses the
// flat config in `eslint.config.cjs`. This file intentionally does not
// declare any `extends` to avoid double-loading configs.
module.exports = {};

